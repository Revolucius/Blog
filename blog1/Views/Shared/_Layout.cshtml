

<!DOCTYPE html>



<head>
    <meta charset="utf-8">
    <title></title>
    <link rel="stylesheet" href="~/Content/css/reset.css">
    <link rel="stylesheet" href="~/Content/css/style.css">
</head>

<body>
    <div class="wrap">
        <div class="left-column">
            <div class="wrap-avatar">
                <img src="~/content/images/avatarg.png" alt="" />
                <h2>Гейб Ньюэлл</h2>
            </div>
            <div class="menu">
                <ul>
@if (@HttpContext.Current.User.Identity.Name != "Admin")
                          {
                    <li>@Html.ActionLink("Admin", "Admin", "Home")</li>
                          }
                    <li>@Html.ActionLink("Archive", "Recent", "Post", new { i = 1 }, null)</li>

                         @if (@HttpContext.Current.User.Identity.Name == "Admin")
                          {
                            <li>
                            @Html.ActionLink("add post", "AddPost", "Home")
                           </li>
                          }
                    </ul>
       @if (@HttpContext.Current.User.Identity.Name == "Admin")
    {
    <form action="@Url.Action("AdminExit", "Home")" method="get">
        <div class="wrap-btn">
            <button class="btn-save">Exit</button>
        </div>
        </form>

        }


            </div>
            
            @Html.Action("Recent", "Post")
          @Html.Action("Recent", "Comment")

   @*  @Html.Action("Recent", "Comment")  @Html.Partial("RecentPosts", Model.RecentPost)
    @Html.Partial("RecentComments", Model.RecentComments)*@
            </div>
        <div class="right-column">
            @RenderBody()
        </div>
    </div>
</body>
